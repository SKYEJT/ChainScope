# ChainScope — deployment requirements for CPU hosts (Streamlit Community Cloud etc.).
#
# Community Cloud picks this file over pyproject.toml. Local GPU development can
# keep using `pip install -e .`, which reads pyproject.toml instead.
#
# The extra index below serves CPU-only torch wheels (~200 MB). Without it the
# default PyPI wheel pulls the CUDA stack (>2 GB) and blows the 1 GB memory
# budget of the free tier before the app even starts.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.9.0+cpu
torch-geometric==2.7.0

langchain==1.3.4
langchain-core==1.4.0
langchain-openai==1.2.2
langchain-community==0.4.2
langgraph==1.2.4

web3==7.16.0
requests>=2.31

networkx==3.4.2
numpy>=2.0,<3
pandas>=2.2
scikit-learn==1.7.2

streamlit==1.58.0
plotly==6.8.0
matplotlib==3.10.9
pyvis==0.3.2

python-dotenv>=1.0
