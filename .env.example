ALCHEMY_API_KEY=
ETHERSCAN_API_KEY=

# ── LLM: any OpenAI-compatible chat endpoint that supports tool calling ──
# DeepSeek:  LLM_BASE_URL=https://api.deepseek.com          LLM_MODEL=deepseek-v4-flash (or deepseek-v4-pro)
# GLM (智谱): LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4  LLM_MODEL=glm-5.1
# Leave LLM_BASE_URL / LLM_MODEL empty to get the GLM-5.1 defaults.
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
# Legacy names (still honoured as fallbacks if the LLM_* ones are empty):
# ZAI_API_KEY=  ZAI_BASE_URL=  ZAI_MODEL=

SEPOLIA_RPC_URL=
WALLET_PRIVATE_KEY=
IPFS_TOKEN=

# Optional: password gate for public deployments (see app.py). Unset = open.
APP_PASSWORD=
